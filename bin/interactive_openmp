#!/bin/bash

#------------------------------------------------------------------------------
#          Harvard University Atmospheric Chemistry Modeling Group            !
#------------------------------------------------------------------------------
#BOP
#
# !MODULE: interactive_openmp
#
# !DESCRIPTION: Shell script to request an interactive session on Odyssey,
#  geared for OpenMP jobs (i.e. all CPUs on the same node).
#\\
#\\
# !CALLING SEQUENCE:
#  interactive CPUS MEMORY-PER-NODE [TIME-IN-MINUTES] [PARTITION]
#
# !REMARKS:
#  Executes the srun command to start an interactive SLURM session.
#  If PARTITION is omitted, then the job will start on the "jacob" partition.
#  If TIME-IN-MINUTES is omitted, then the job will run for 60 minutes.
#
# !REVISION HISTORY: 
#  28 Oct 2014 - R. Yantosca - Initial version
#  10 Jul 2015 - R. Yantosca - Now print the input arguments to stdout
#  01 Sep 2015 - R. Yantosca - Now pass the partition as the 4th argument
#  10 Nov 2015 - R. Yantosca - Now use --mem instead of --mem-per-cpu
#EOP
#------------------------------------------------------------------------------
#BOC

# Process the input arguments.  Use defaults if the
# user supplies less than 3 argumetnts. (bmy, 7/10/15)
if [[ $# == 4 ]]; then
   cpus=$1
   mem=$2
   time=$3
   part=$4
elif [[ $# == 3 ]]; then
   cpus=$1
   mem=$2
   time=$3
   part=jacob
elif [[ $# == 2 ]] ; then
   cpus=$1
   mem=$2
   time=60
else
   echo "Usage: interactive_openmp CPUS MEM-PER-CPU [TIME-IN-MINUTES]"
   exit 1
fi

# Print information
echo "%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%"
echo "%%% Starting an Odyssey interactive session!"
echo "%%%"
echo "%%% Number of CPUs      : $cpus"
echo "%%% Number of Nodes     : 1"
echo "%%% Memory per node     : $mem MB"
echo "%%% Requested time      : $time minutes"
echo "%%% Requested partition : $part"
echo "%%%"
echo "%%% Reminder: Make sure \$OMP_NUM_THREADS in your shell is set"
echo "%%% to the same number of CPUs that you requested!"
echo "%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%"

# Start the interactive session
srun --pty --x11=first -n $cpus -N 1 --mem=$mem -t $time -p $part /bin/bash

#EOC